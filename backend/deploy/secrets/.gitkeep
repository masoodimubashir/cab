# Place your Firebase service-account JSON here as `firebase.json`.
# Download: Firebase Console (project dreamcabs-c851f) → Project settings →
# Service accounts → Generate new private key. Rename it to firebase.json.
# This folder is git-ignored except for this placeholder — never commit secrets.
